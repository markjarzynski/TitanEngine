{
    "tasks": [
        {
            "type": "shell",
            "label": "cmake build active file",
            "command": "cmake --build ${workspaceFolder}/build --config Debug --target ${command:cmake.launchTargetPath}",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}